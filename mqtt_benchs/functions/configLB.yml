- default:
    - workers:
        - set:
- DB:
    - workers:
        - wrk: "edgeworker"
          invalidate:
            - capacity_used: 50
        - wrk: "worker"
      strategy: best-first
- Cloud:
    - controller: "controller"
      workers:
        - set:
      topology_tolerance: "none"
      strategy: random
  followup: fail
- MQTT:
    - controller: "edgecontroller"
      workers:
        - set:
      topology_tolerance: "none"
      strategy: random
  followup: fail
